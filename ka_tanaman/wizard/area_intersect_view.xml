<odoo>
	<data>
		<record model="ir.ui.view" id="intersect_wizard">
            <field name="name">Intersect Wizard</field>
            <field name="model">ka_plantation.area.intersect</field>
            <field name="arch" type="xml">
                <form string="raise error">
	                <group>
                        <field name="name" nolabel="1"/>
	                </group>
	                <footer>
	                 	<button name="reload" type="object" string="OK"/>
	                </footer>
	            </form>
            </field>
        </record>

        <!-- <record id="action_intersect_wizard" model="ir.actions.act_window">
            <field name="name">Area Intersect</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">ka_plantation.area.intersect</field>
            <field name="view_type">form</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="intersect_wizard"/>
            <field name="target">new</field>
        </record> -->
	</data>
</odoo>